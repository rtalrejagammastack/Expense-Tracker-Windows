<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">

    <!-- Left side of navbar -->  
    <%= link_to "Home Page",root_path,class:"navbar-brand" %>
    <%# <a class="navbar-brand" href="#">
      <img src="your-icon.png" width="30" height="30" class="d-inline-block align-top" alt="">
    </a> %>
    
    <!-- Center of navbar -->
    <div class="mx-auto">
      <h5 class="text-center mb-0">Cash Flo</h5>
    </div>

    <!-- Right side of navbar -->
    <div class="ml-auto">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= current_user.email %>
          </li>
          <li class="nav-item">
            <%= link_to "Logout",destroy_user_session_path,class:"nav-link",method: :delete %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login",new_user_session_path,class:"nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Signup",new_user_registration_path,class:"nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>